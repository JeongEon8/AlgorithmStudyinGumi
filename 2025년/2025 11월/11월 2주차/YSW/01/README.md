# [백준 - G4] 1253. 좋다 (Easy)

## ⏰ **time**

50분

## :pushpin: **Algorithm**

- 자료 구조
- 정렬
- 이분 탐색
- 두 포인터

## ⏲️**Time Complexity**

$O(N^2)$

## :round_pushpin: **Logic**

N개의 수를 정렬을 한 다음
N개의 수 중에서 어떤 수가 다른 수 두 개의 합으로 나타 날 수 있는지 검사를 한다.
두개의 수를 양쪽 끝에서 부터 검사를 해서 두수를 더 했을때, 어떤 수 보다 크다면 오른쪽의 포인터가 왼쪽으로 한칸 이동하고
작다면 왼쪽의 포인터가 오른쪽으로 한 칸 이동한다.
왼쪽의 포인터나 오른쪽의 포인터가 어떤수를 가르키게 되면 계산하지 않는다.
이 과정을 왼쪽의 포인터가 오른쪽의 포인터보다 커지게 될때 까지 반복하고 어떤수가 나오면 count를 증가하고 다른 수를 검사한다.

```java
		for (int i = N - 1; i >= 2; i--) {
			int target = number[i];
			int right = N - 1;
			int left = 0;
			while (left < right) {
				if (right == i) {
					right--;
					continue;
				} else if (left == i) {
					left++;
					continue;
				}

				int sum = number[left] + number[right];

				if (sum == target) {
					count++;
					break;
				} else if (sum > target) {
					right--;
				} else {
					left++;
				}
			}
		}
```

## :black_nib: **Review**

## 📡**Link**

https://www.acmicpc.net/problem/1253
