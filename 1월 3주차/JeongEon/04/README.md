# [백준 - 실버 3] 7795. 먹을 것인가 먹힐 것인가

## ⏰ **Time**
약 15분

## :pushpin: **Algorithm**
정렬

## ⏲️ **Time Complexity**
$O(N \log N + M \log M + NM)$

## :round_pushpin: **Logic**

1. 테스트 케이스 수 `t`를 입력받는다.
2. 각 테스트 케이스마다:

   * A 집합(`v1`), B 집합(`v2`)의 크기를 입력받는다.
   * 두 집합의 원소를 각각 벡터에 저장한다.
3. 두 벡터를 오름차순 정렬한다.
4. A의 각 원소에 대해:

   * B의 원소를 앞에서부터 순회하며
   * `A[i] > B[j]`인 경우 카운트를 증가시킨다.
   * 정렬되어 있으므로 `A[i] <= B[j]`가 되는 순간 반복을 중단한다.
5. 최종 카운트를 출력한다.

```
A, B 정렬
for A의 각 원소:
    for B의 원소 앞에서부터:
        if A[i] > B[j]:
            cnt++
        else:
            break
```

## :black_nib: **Review**
-

## 📡 Link

[https://www.acmicpc.net/problem/7795](https://www.acmicpc.net/problem/7795)
