# [백준 - S2] 20153. 영웅이는 2의 거듭 제곱을 좋아해! 영웅이는 2의 거듭 제곱을 좋아해! (Hard)

## ⏰ **time**

50분

## :pushpin: **Algorithm**

- 수학
- 브루트포스 알고리즘
- 비트마스킹

## ⏲️**Time Complexity**

$O(N)$

## :round_pushpin: **Logic**

N개의 숫자 중 최대 한 개를 제외했을 때 만들 수 있는 XOR 합의 최댓값을 구하는 문제이다.
문제에서 말하는 “이진수 각 자릿수의 합이 홀수이면 더한다”는 규칙은 비트별 XOR 연산과 동일하다.

먼저 모든 숫자를 XOR 하여 xorSum을 구하면, 이는 아무것도 제외하지 않은 경우의 값이다.
XOR은 같은 수를 두 번 연산하면 상쇄되는 성질이 있으므로
xorSum ^ nums[i]는 i번째 숫자를 제외한 XOR 값이 된다.

모든 숫자에 대해 이 값을 비교하여 가장 큰 값을 정답으로 선택한다.

```java
		for (int i = 0; i < N; i++) {
			nums[i] = Integer.parseInt(st.nextToken());
			xorSum = xorSum ^ nums[i];
		}

		int max = xorSum;
		for (int i = 0; i < N; i++) {
			max = Math.max(max, xorSum ^ nums[i]);
		}
```

## :black_nib: **Review**

## 📡**Link**

https://www.acmicpc.net/problem/20153
