# [백준 - S3] 21967. 세워라 반석 위에 (Easy)

## ⏰ **time**

40분

## :pushpin: **Algorithm**

- 브루트포스 알고리즘
- 두 포인터

## ⏲️**Time Complexity**

$O(N)$

## :round_pushpin: **Logic**

이 문제는 수열에서 최댓값과 최솟값의 차이가 2 이하인 가장 긴 연속 구간을 찾는 문제입니다.
이를 해결하기 위해 투 포인터(슬라이딩 윈도우) 기법을 사용했습니다.
숫자의 범위가 1에서 10으로 매우 작기 때문에, 현재 윈도우에 포함된 숫자의 개수를 저장할 int[11] 크기의 배열(numCounts)을 사용했습니다. 이를 통해 구간 내 최댓값과 최솟값을 $O(1)$에 가깝게 찾아낼 수 있습니다.
우선 right 포인터를 한 칸씩 이동하며 숫자를 카운트하고 윈도우를 확장합니다.
확장된 윈도우의 max - min 차이가 2를 초과하면, 조건을 만족할 때까지 left 포인터를 오른쪽으로 밀면서 기존 숫자의 카운트를 줄여나갑니다.
매 단계마다 조건을 만족하는 최장 길이를 maxLength 변수에 업데이트합니다.

```java
		for (int right = 0; right < n; right++) {
			numCounts[nums[right]]++;
			while (true) {
				int max = 0;
				int min = 11;
				for (int i = 1; i <= 10; i++) {
					if (numCounts[i] != 0) {
						max = Math.max(max, i);
						min = Math.min(min, i);
					}
				}
				if (max - min <= 2) {
					break;
				}

				numCounts[nums[left]]--;
				left++;
			}

			maxLength = Math.max(maxLength, right - left + 1);
		}
```

## :black_nib: **Review**

## 📡**Link**

https://www.acmicpc.net/problem/21967
